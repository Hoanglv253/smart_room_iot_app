name: smart_room_iot_app
description: Smart room authentication app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  cloud_firestore: ^6.4.1
  cupertino_icons: ^1.0.8
  firebase_auth: any
  firebase_core: any

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
